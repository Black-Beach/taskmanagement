# 要件定義書:タスク管理ボード(仮)

画面構成・データベース設計の詳細は [基本設計書](design.md) を参照。

## 1. 概要

| 項目 | 内容 |
|---|---|
| アプリ名 | タスク管理ボード(仮) |
| 概要 | 自分の勉強や作業のタスクを、カードで見える化するアプリ(Trello風) |
| 利用者 | 自分ひとり(ログイン・共有は不要) |
| 目的 | スクールの課題として提出する |

## 2. 背景・課題

普段の学習・作業タスクを Excel や付箋で管理していたが、以下の課題があった。

- タスクの優先度や期限が一覧で把握しづらい
- 完了/未完了の状態が視覚的にわかりにくい
- Trello 等の既存ツールはアカウント作成やログインが必要で、ひとりで使うには機能が過剰

これらを解消しつつ、以下の技術を習得することを目的として、本アプリをスクール課題として制作する。

- DOM 操作、ドラッグ&ドロップによる UI 操作
- データベース(DB)を用いたデータ永続化と API 設計
- カードの CRUD(作成・読み取り・更新・削除)処理

## 3. ユースケース

| ID | 名称 | 基本フロー |
|---|---|---|
| UC-01 | タスクを登録する | 「カードを追加」ボタンを押す→モーダルにタイトル・詳細・期限・優先度を入力→保存すると「未着手」列にカードが追加される |
| UC-02 | タスクの進捗を更新する | カードをドラッグし、進めたい列(進行中/完了)にドロップする→カードの状態が更新され、DBに保存される |
| UC-03 | タスクを編集・削除する | カードをクリックしてモーダルを開く→内容を編集して保存、または削除する |
| UC-04 | タスクを検索・絞り込む | 検索欄にキーワードを入力、または優先度・期限状態・ラベルで絞り込む→条件に合うカードのみ表示される |

- アクターはすべて「利用者(自分ひとり)」。ログインや権限の違いはない。

## 4. 機能要件

### 4.1 カード管理

| ID | 機能 | 優先度 |
|---|---|---|
| F-01 | カードを追加できる(タイトル、詳細、期限、優先度) | 必須 |
| F-02 | カードを編集できる | 必須 |
| F-03 | カードを削除できる | 必須 |
| F-04 | 「未着手 / 進行中 / 完了」の3列(固定)にカードを表示する | 必須 |
| F-05 | ドラッグ&ドロップでカードを列間移動できる | 必須 |
| F-10 | 同じ列内でカードの並び順を入れ替えられる | 必須 |
| F-06 | データがリロード後も残る(データベースに保存) | 必須 |
| F-07 | ラベル色でカードを色分けできる | できれば |

### 4.2 期限の表示

| ID | 機能 | 優先度 |
|---|---|---|
| F-08 | 期限の当日・前日のカードを目立たせる(例:オレンジ)。完了列は除く | 必須 |
| F-09 | 期限を過ぎたカードをグレーアウトし、「期限切れ」と表示する。完了列は除く | 必須 |

| 状態 | 条件 | 表示 |
|---|---|---|
| 通常 | 期限まで2日以上、または期限なし | 通常表示 |
| 期限間近 | 期限の当日・前日 | 目立たせる色 |
| 期限切れ | 期限を過ぎている | グレーアウト+「期限切れ」の文字 |
| 完了 | 「完了」列にある | 期限に関係なく通常表示 |

### 4.3 検索・絞り込み

| ID | 機能 | 優先度 |
|---|---|---|
| F-11 | タイトルと詳細を対象に、キーワードで検索できる | できれば |
| F-12 | 優先度(高/中/低)で絞り込める | できれば |
| F-13 | 期限の状態(期限間近 / 期限切れ)で絞り込める | できれば |
| F-14 | ラベル色で絞り込める(F-07 を実装した場合のみ) | できれば |

- 条件に合わないカードは非表示にする。
- 検索と絞り込みは組み合わせて使える(すべての条件を満たすカードだけ表示)。

## 5. 画面構成

画面は1枚のボード画面で、「未着手 / 進行中 / 完了」の3列を横に並べて表示する。カードの追加・編集はモーダルで行い、検索欄・絞り込みはボード上部に置く。
詳細な画面構成・ワイヤーフレームは [基本設計書](design.md#1-画面構成) を参照。

## 6. データ項目(カード)

カードはタイトル・詳細・期限・優先度・ラベル色・状態(列)・並び順の各項目を持つ。
詳細なデータ項目一覧・ER図は [基本設計書](design.md#2-データ項目カード) を参照。

## 7. 非機能要件

| 項目 | 内容 |
|---|---|
| 対象端末 | PCのみ |
| 対応ブラウザ | 最新のChrome、Edge、Firefox、Safari |
| データ保存先 | データベース(RDBMS)。フロントエンドから API 経由でアクセスする |

## 8. 対象外(やらないこと)

- ログイン・ユーザー管理
- 複数ボード、他人との共有
- スマホ・タブレット対応
- 列の追加・名前変更
- 通知(メール、ブラウザ通知)

## 9. 未決事項

- アプリ名の決定
- 絞り込み中にドラッグ&ドロップで並び替えたとき、非表示カードとの相対位置をどう扱うか(実装時に決定)
- 使用するDB製品(例:SQLite / MySQL / PostgreSQL など)
